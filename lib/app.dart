import 'package:flutter/material.dart';

class App extends MaterialApp {
  const App({Key? key})
      : super(
            key: key,
            home: const Scaffold(
              body: Text('x'),
            ));
}
